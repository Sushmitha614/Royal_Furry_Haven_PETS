import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  dashboardContainer: {
    padding: '20px',
    background: '#f4f4f4',
    borderRadius: '10px',
  },
}));

export default useStyles;
